Explain the file, command, or code referenced in the argument (or the current
selection / open file if none is given) in plain, concise terms.

Cover:
1. **What it does** — a one-paragraph summary of its purpose.
2. **How it works** — walk through the key parts step by step (flags, options,
   control flow, side effects). Flag anything that touches the filesystem,
   network, or system state.
3. **Gotchas** — surprising behavior, footguns, platform differences, or
   destructive operations to be careful with.

Keep it tight and skip the obvious. If the target is ambiguous, ask which one.
